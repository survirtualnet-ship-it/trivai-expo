import { MyBusinessScreen } from '@/src/company/screens/MyBusinessScreen'

/** Tab: Mi Negocio — visible only for business users (see tabs _layout). */
export default function MiNegocioTab() {
  return <MyBusinessScreen />
}
