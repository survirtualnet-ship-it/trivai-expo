/** Re-export for `@/hooks/useAuthStore` imports. */
export {
  useAuthStore,
  useAuthHydrated,
} from '@/src/auth/store/useAuthStore'

export type { AuthUser, AuthRole, LoginPayload } from '@/src/auth/types'
